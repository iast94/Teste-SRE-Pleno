# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Folder-specific ignore
.idea/
.vscode/
.history/
node_modules/
# Helm specific
/charts/*.tgz
# Segredos e arquivos temporários do projeto
secrets/
*.gotmpl
*.bak